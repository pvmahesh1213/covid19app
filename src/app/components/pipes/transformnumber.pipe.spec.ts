import { TransformnumberPipe } from './transformnumber.pipe';

describe('TransformnumberPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformnumberPipe();
    expect(pipe).toBeTruthy();
  });
});
